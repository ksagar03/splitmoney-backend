# splitmoney-backend
SplitMoney 2.0 is a backend application built with Spring Boot using a monolithic architecture. It is designed to manage group-based expenses, allowing users to create groups, track shared expenses, and calculate balances between members. The project is currently under active development.
